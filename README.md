# Mini-Project
Built a React-based application that integrates the GitHub API to display user followers and repositories with dynamic routing using React Router.
## Features
- Fetches GitHub followers
- Displays public repositories
- Dynamic routing with React Router
- Clickable repository links
